# L0
The following were added
* call
* address

`Abstract` was removed

Procedure is now defined in the language. The program runs a series of procedures, which each run their own statements. Addresses are used instead of variables.